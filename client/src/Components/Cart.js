import React from "react";

const Cart = (props) => {
    const {cartItems} = props.location.cartItems;
    console.log (cartItems);
    
      return (
        <>
        hello beenish

        </>
    )
}

export default Cart;